break和continue的区别
    break结束整个循环
    continue结束单次循环，也就是continue后的循环语句不再执行，继续运行下一次循环语句
break和continue必须写在循环体内，break也可以写在switch语句内
for(i=0;i<10;i++){
    if(i%2==0)
        continue;
}
判断奇偶数 i=10

goto语句
    标识；
    goto标识；
数组：连续存储多条同类型的数据结构
声明：
    类型 数组变量名[存储的数量个数]；
初始化：
    int num[10]={};
    int num[10]={0};
    全部初始化为0
    int num[10]={1,2};
    int num[3]={1,2,3};
char buf[23]="1234asd";
    int int[2];
    num[2]={1,4};
注意：
    数组名不是数据，单独写数组名表示数组的首地址是一个常量，不能对数组名进行赋值操作
    sizeof(buf)=23;//1*23
    int num[4];
    sizeof(num)=16;//常量
    int *arr;
    sizeof(arr)=4;//变量

数组元素才是数据；
数组加对应的下标才是数据
int num[23]={3,4,6};
num-代表数组的首地址，不是数据
num[3]-代表数组的第四个数据，是数据
对数组的数据访问实际上是对数组下标的改变
下标-表示数据在数组中的位置
下标是从0开始
数组下标超范围不一定会出错，但是存在隐患。在C语言中叫做数组越界，在编译时不会出错，会在运行时出错，段错误-非法访问地址
int num[5];
num-不是数据
num[i]-是数据，int型
i下标为0-4

1,V,Shift+g,=    整齐代码
