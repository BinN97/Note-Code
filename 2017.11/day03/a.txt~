2017-11-16
通配符：
     ?：代表任意一个字符
     *：代表任意多个字符
     >>：写入到文件
rm -r q?w
rm -r q*
rm -r *
cat：查看文件内容
tar zcvf：压缩
tar zxvf：解压缩
vim  记事本打开
   vim:
   vim 文件名：文件存在则打开，不存在则创建
          命令模式
IAOiao            ：/?
ESC               doubleESC
编辑模式     NO    底行模式
在命令模式下：
   iaoIAO：进入到编辑模式
   :/?   ：进入到底行模式
   yy    ：复制光标当前所在行
   Nyy   ：复制N行
   dd    ：删除/剪切光标当前所在行
   Ndd   ：删除N行
   p     ：粘贴
   u     ：撤销上一步
   x     ：删除一个字符
   ZZ    ：保存退出
   G     ：光标回到底行
   NG    ：光标回到N行
在底行模式（：）
   w     :保存
   w 文件名 ：另存
   q     ：不保存退出
   wq    ：保存退出
   q！   ：强制退出
   x     ：保存退出
   N     ：光标跳转到N行
